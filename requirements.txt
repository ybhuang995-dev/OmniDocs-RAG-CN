chromadb>=1.5.5
sentence-transformers>=5.0.0
fastmcp>=3.1.0
rank-bm25>=0.2.2

# Web Dashboard
fastapi>=0.135.0
uvicorn>=0.42.0

# Web crawling & HTML parsing
httpx>=0.28.0
beautifulsoup4>=4.12.0
html2text>=2024.2
lxml>=5.0
trafilatura>=2.0

# [中文化] 中文支持依赖
readability-lxml>=0.8       # Mozilla Readability — 语言无关网页正文提取
jieba>=0.42                  # 中文分词（BM25 关键词搜索用）

# Binary document formats
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1
python-pptx>=0.6.23

# File watcher (auto-reindex on file changes)
watchdog>=4.0

# Optional: GPU acceleration (pip install torch)
# Set RAG_DEVICE=cuda to enable

# Optional: JS-rendered sites (pip install playwright && playwright install chromium)
